# Audit 2026-04-04

## Scope

This audit validated the latest `main` on `hpc1` after the refactor-aligned update documented in
`comet-docs`, then applied, committed, pushed, and revalidated a follow-up fix for the infer-runtime
build regression found during the first pass.

Completed in this slice:

- connected to `hpc1`
- updated `/home/baal/comet-node` from `8e7646b` to `cb1ff81` via `git pull --ff-only`
- reconfigured the Linux x64 build in the shared-storage-backed build root
- built the controller and the refactor-touched test targets
- found a `comet-inferctl` link failure
- fixed the link graph in `CMakeLists.txt`
- committed and pushed the fix as `d7975a0`
- repaired `origin` SSH access on `hpc1`
- pulled the committed fix back to `hpc1` from `origin`
- rebuilt `comet-inferctl` on `hpc1`
- reran representative tests and a minimal infer CLI smoke
- ran a safe live smoke against the active controller and plane state on `hpc1`

Environment facts:

- host: `hpc1`
- validation time: `2026-04-04T15:15:45+02:00`
- final validated commit on `hpc1`: `d7975a0`
- final validated subject: `Fix comet-inferctl model adapter linkage`

Operational note:

- initial state:
  - `origin` on `hpc1` was configured as `git@github.com:RayGron/comet-node.git`
  - direct `git fetch` from `origin` failed on `hpc1` because the configured key was not
    authorized in GitHub
  - the first update therefore used:
  - `git pull --ff-only https://github.com/RayGron/comet-node.git main`
- final state:
  - SSH authentication to GitHub now works on `hpc1`
  - standard `git fetch origin --prune` and `git pull origin main` work again

## Result

Primary outcome:

- the targeted `hpc1` validation now passes after the `comet-inferctl` link fix
- the initial infer-runtime build blocker was real and is now fixed in committed code

Current release posture:

- no blocking build failures remain in the refactor-touched areas covered by this audit
- the safe live smoke performed in this slice was positive
- full production confidence would still benefit from a broader runtime matrix, but there is no
  blocking signal from the current `hpc1` checks

## Findings

### 1. `comet-inferctl` link regression was fixed

Severity: high during discovery, fixed in follow-up

Initial failure:

- target: `comet-inferctl`
- failure point: final link step
- error family: `undefined reference` to `comet::runtime::ModelAdapter::*`

Root cause:

- `common/src/runtime/model_adapter.cpp` was added to `comet-common`
- `comet-inferctl` started using `ModelAdapter` from `infer_app.cpp` and
  `llama_rpc_runtime.cpp`
- `comet-inferctl` did not link `comet-common`

Fix applied:

- updated `comet-inferctl` to link `comet-common`

Relevant files:

- [CMakeLists.txt](/Users/vladislavhalasiuk/Projects/Repos/comet-node/CMakeLists.txt#L141)
- [CMakeLists.txt](/Users/vladislavhalasiuk/Projects/Repos/comet-node/CMakeLists.txt#L149)
- [CMakeLists.txt](/Users/vladislavhalasiuk/Projects/Repos/comet-node/CMakeLists.txt#L533)
- [CMakeLists.txt](/Users/vladislavhalasiuk/Projects/Repos/comet-node/CMakeLists.txt#L558)
- [runtime/infer/src/infer_app.cpp](/Users/vladislavhalasiuk/Projects/Repos/comet-node/runtime/infer/src/infer_app.cpp)
- [runtime/infer/src/runtime/llama_rpc_runtime.cpp](/Users/vladislavhalasiuk/Projects/Repos/comet-node/runtime/infer/src/runtime/llama_rpc_runtime.cpp)
- [common/src/runtime/model_adapter.cpp](/Users/vladislavhalasiuk/Projects/Repos/comet-node/common/src/runtime/model_adapter.cpp)

Follow-up validation:

- `comet-inferctl` rebuilt successfully on `hpc1`
- binary artifact exists and links against the expected CUDA/runtime libraries
- `build/linux/x64/comet-inferctl list-profiles` succeeds when run from the repo root

### 2. `hpc1` Git SSH transport was repaired

Severity: medium during discovery, fixed in follow-up

Initial observed behavior:

- `git fetch --all --prune` on `hpc1` failed with `git@github.com: Permission denied (publickey)`

Cause:

- `~/.ssh/config` on `hpc1` forced `github.com` to use an unauthorized key

Fix applied:

- installed a working GitHub key on `hpc1`
- updated `~/.ssh/config` so `github.com` uses the working key first

Validation:

- `ssh -T git@github.com` returns `Hi RayGron!`
- `git ls-remote origin HEAD` works
- `git fetch origin --prune` works

Impact before fix:

- routine `pull` from the configured remote is not reliable on the host
- deployment flow currently depends on an HTTPS override rather than the saved remote config

Status:

- fixed on `hpc1`
- operational only; no repo code change was required

### 3. `comet-inferctl` asset lookup is cwd-sensitive

Severity: low

Observed behavior:

- running `./comet-inferctl list-profiles` from `build/linux/x64` failed with:
  - `runtime profiles not found: runtime/infer/runtime-profiles.json`
- running `build/linux/x64/comet-inferctl list-profiles` from the repo root succeeded

Impact:

- ad-hoc CLI use depends on the current working directory unless the caller uses the expected repo
  root layout

Status:

- not blocking for the current validation
- worth keeping in mind for operator tooling and local debugging UX

## Validation Results

### Rebuilt targets

Successfully built on `hpc1`:

- `comet-model-adapter-tests`
- `comet-state-v2-projector-tests`
- `comet-compose-renderer-tests`
- `comet-controller-apply-state-tests`
- `comet-plane-skills-tests`
- `comet-skills-factory-tests`
- `comet-controller`
- `comet-inferctl`

### Executables run on `hpc1`

Passed:

- `comet-model-adapter-tests`
- `comet-state-v2-projector-tests`
- `comet-compose-renderer-tests`
- `comet-controller-apply-state-tests`
- `comet-plane-skills-tests`
- `comet-skills-factory-tests`

Binary smoke:

- `comet-controller` starts and prints usage
- `build/linux/x64/comet-inferctl list-profiles` prints the runtime profiles when run from the repo
  root

### Live smoke on `hpc1`

Passed:

- `systemctl is-active comet-node-controller.service` -> `active`
- `systemctl is-active comet-node-hostd.service` -> `active`
- `curl http://127.0.0.1:18080/health` -> `status=ok`
- SQLite read-only plane summary:
  - `lt-cypher-ai` -> `running`, `generation=56`, `applied_generation=56`
  - `maglev` -> `running`, `generation=1`, `applied_generation=1`
- `GET /api/v1/planes/lt-cypher-ai/interaction/status` returned `ready=true`
- returned runtime state for `lt-cypher-ai` confirms:
  - `runtime_backend=llama-rpc-head`
  - `runtime_phase=running`
  - `gateway_ready=true`
  - `skills_ready=true`

Expected protected behavior:

- `GET /api/v1/planes/maglev/interaction/status` returned `401`
- `GET /api/v1/planes/maglev/interaction/models` returned `401`

Interpretation:

- the protected `maglev` plane is enforcing auth rather than failing open
- the unprotected/accessible `lt-cypher-ai` plane reports a healthy live interaction path

### Coverage Notes

This audit intentionally validated the areas touched by commit `cb1ff81` and the follow-up link
fix:

- new shared model-adapter code
- projector and compose-renderer integration
- controller interaction/service refactor surface
- infer runtime buildability and basic CLI bootability

Not completed in this slice:

- authenticated interaction checks against protected planes such as `maglev`
- end-to-end inference request/response validation with a real prompt
- full repo-wide test executable matrix

Reason:

- this pass prioritized fixing the build break, restoring normal Git transport on `hpc1`, and
  confirming non-invasive live readiness

## Recommended Next Step

1. If needed, run an authenticated interaction smoke against `maglev` to validate the protected
   path end-to-end.
2. Run a real chat/completions request against `lt-cypher-ai` or another non-production-safe test
   plane when prompt-level inference validation is desired.
3. Expand from the targeted matrix here to a wider repo-wide executable test sweep when time allows.
